return {
  {
    "rcarriga/nvim-notify",
    opts = {
      -- Modify opts of the notify plugin to only pop up on errors
      level = 4,
      render = "minimal",
      stages = "static",
    },
  },
}
